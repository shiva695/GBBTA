//@import Constants
import { constant } from "../../../constants/reduxConstants";

const actionTypes = {
  CONFIG_GET_REQUEST: `${constant.CONFIGSLICE}/getConfigRequestAction`,
};

export default actionTypes;
